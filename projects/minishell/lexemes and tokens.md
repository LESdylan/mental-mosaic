tokens are the smallest meaningful units of a programming language, such as keywords (`if`, `while`) identififers (variable names), operators (`+`, =) and  [[literals]]

[[Understand token in an interpreter]]